import isObj from './isObj';
import moduleName from './isArray';
/**
 * @description 判断是否为空 object|string|array
 *
 * @param {*} v 任意值
 * @return {*}  {boolean} 返回一个布尔值
 * @example empty("") => true empty(null) => true empty(undefined) => true empty({}) => true empty([]) => true
 */
const empty = (v:any):boolean => {
    return false
}