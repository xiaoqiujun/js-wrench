/**
 * @description 迭代器
 *
 * @param {*} collection
 * @param {Function} iteratee
 * @example each([1,2,3], (v, i, arr) = {
 *
 *          })
 */
declare const each: (collection: any, iteratee: Function) => void;
export default each;
